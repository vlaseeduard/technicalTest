### The project needs to have serveral groups inserted in db and 2-3 staff users.


python manage.py makemigrations
python manage.py migrate
# load the groups
python manage.py loaddata db_init/fixtures.json

